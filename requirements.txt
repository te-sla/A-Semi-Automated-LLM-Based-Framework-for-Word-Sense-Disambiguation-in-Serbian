# requirements.txt
# Use PyTorch with CUDA 12.6 wheels
# Replace the CUDA index URL with the one matching your local GPU/CUDA setup if needed
--extra-index-url https://download.pytorch.org/whl/cu126

# Core deps for WSD, HF/PEFT fine-tuning, LangChain + LLM integrations, viz & notebooks
transformers>=4.40.0
peft>=0.15.2
langchain>=0.0.235
langchain-openai>=0.0.2
langchain-google-genai>=0.0.5
langchain-community>=0.0.2
langgraph>=0.1.0
openai>=1.5.0
google-genai>=0.2.0
ollama>=0.1.0
accelerate>=0.20.1
huggingface_hub>=0.10.0

# CUDA-enabled PyTorch (cu121 build)
torch>=2.7.0

# Data & visualization
numpy>=1.24.3
pandas>=2.0.3
matplotlib>=3.7.1
seaborn>=0.13.2
dash>=2.10.0
tqdm>=4.65.0

# Excel engine
openpyxl>=3.0.10

# Evaluation & ML tools
evaluate>=0.4.0
scikit-learn>=1.3.0
# Pinned for simple_wsd reproducibility; update deliberately alongside embedding usage
sentence-transformers==3.0.1

# Config & serialization
python-dotenv>=1.0.0
pyyaml>=6.0.0
safetensors>=0.5.3
certifi>=2024.0.0
hf_xet>=0.1.0  # Fast model downloads via Xet Storage

# Notebook support
jupyter>=1.0.0
ipykernel>=6.24.0

#custom converter
git+https://github.com/te-sla/webanno-spacy-converter.git#egg=webanno-spacy-converter
